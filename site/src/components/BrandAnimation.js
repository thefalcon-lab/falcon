/** @jsx jsx */
import { jsx, Container, Flex, Box } from 'theme-ui'
import { Brand } from '../svg/all-icons'

const BrandAnimation = () => {
  return (
    <Box sx={{ bg: 'black' }}>
      <Brand sx={{}} />
    </Box>
  )
}

export default BrandAnimation
